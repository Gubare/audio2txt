# audio2txt (Long Audio Support)

Простой и надёжный скрипт для транскрибации длинных аудиофайлов (более 1 часа) с помощью OpenAI Whisper. Код автоматически разбивает аудио на чанки, обрабатывает их по очереди и сохраняет результат в текстовый файл.

> 💡 Идеально подходит для Colab, локальных машин и полевых условий — не требует сложной установки или GPU (хотя GPU значительно ускоряет процесс).

---

## ✅ Возможности

- Поддержка форматов: `.m4a`, `.mp3`, `.wav`, `.flac` и др.
- Автоматическая сегментация длинного аудио на чанки (по умолчанию 20 минут).
- Устойчивость к ошибкам: если один чанк не обработался — продолжает работу.
- Работа как в Google Colab, так и локально.
- Минимальные зависимости.

---

## ⚙️ Требования

### Для Google Colab:
```bash
!pip install pydub openai-whisper
!apt install ffmpeg

## 🚀 Как использовать
### В Google Colab:
- Загрузите этот ноутбук или скопируйте код.
- Включите GPU: Runtime → Change runtime type → Hardware accelerator → GPU.
- Загрузите ваш аудиофайл через files.upload().
- Запустите ячейки.
- Cкачайте результат (output.txt).
### Локально:
- Скопируйте transcribe_long_audio.py в свою папку.
- Замените audio_path = "your_file.m4a" на путь к вашему файлу.
- Запустите
```bash
python transcribe_long_audio.py

## 🛑 Ограничения
Не определяет спикеров (только текст).
Не выполняет диаризацию (разделение по говорящим).
Для очень длинных файлов (>2 часов) может потребоваться увеличение chunk_duration_ms.
## 📜 Лицензия
MIT License — свободное использование, модификация и распространение.

## 🙏 Благодарности
OpenAI Whisper — за мощную модель транскрибации.
pydub — за простую работу с аудио.
Google Colab — за бесплатный доступ к GPU.
